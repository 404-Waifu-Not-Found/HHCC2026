# ClipQuest live QA and hardening — 2026-07-31

## Final production acceptance update

This section supersedes the interrupted-status notes later in this historical log.

- GitHub branch `codex/duolingo-ui-rebuild` is current through commit `7c19b26`.
- Cloudflare production version `ceb71660-7b11-4a4b-a67c-cb1eed2bf473` is live at `https://clipquest.ccwu.cc`.
- `/health` returned ready with authentication, generation, email, and YouTube-encryption configuration present. Secret values were never printed or committed.
- The unapproved `youtube-transcript.ai` experiment was removed from server, browser, and test code before deployment. Production CSP remains same-origin for application connections.
- Two isolated production QA accounts were disabled after acceptance: both are banned, both have role `user`, and both have zero remaining sessions. The seeded AP lesson remains as non-secret QA evidence.

### Auth, theme, and responsive acceptance

Real Chrome testing used the logged-in Rich profile; Safari was not used.

- A synchronous, self-hosted preflight now resolves saved/system light or dark mode and desktop/mobile class before the Expo UI mounts.
- Desktop Chrome at 1,637 px reported `data-cq-device="desktop"`; a temporary 390 × 844 Chrome viewport reported `data-cq-device="mobile"` before sign-up rendered.
- Light mode resolved to `rgb(246, 248, 252)` and dark mode to `rgb(8, 15, 37)`. Both switched through the visible Settings controls. The final preference was restored to System.
- Reduced motion was exercised and restored off.
- The auth message is now “Turn serious videos into lasting knowledge,” with evidence-based supporting copy.
- Sign-in and sign-up labels are gray in-field placeholders. Username, email, and password each stopped matching `:placeholder-shown` after real typing.
- The 390 × 844 sign-up layout had no document-level horizontal overflow.

Captured evidence:

- `docs/screenshots/live-qa/2026-07-31/sign-in-desktop-light.jpg`
- `docs/screenshots/live-qa/2026-07-31/sign-up-desktop-dark.jpg`
- `docs/screenshots/live-qa/2026-07-31/sign-up-mobile-390x844-dark.jpg`
- `docs/screenshots/live-qa/2026-07-31/admin-system-desktop-dark.jpg`

### AP-level source and lesson acceptance

The live run used advanced-placement-level material, not children’s videos:

- YouTube reference: `https://www.youtube.com/watch?v=PFbYpsUV-4c` — “Photosynthesis - Light-dependent Stage - Post 16 Biology (A Level, Pre-U, IB, AP Bio)”.
- YouTube import exercised: `https://www.youtube.com/watch?v=Le7KOX91w7U` — “Photosynthesis Intro and Light-Dependent Reactions”.
- bilibili reference: `https://www.bilibili.com/video/BV12rgX6HEcu/` — `光合作用｜AP生物㉑`.

Observed results:

- Invalid and unsupported URL feedback passed.
- YouTube metadata import passed for `Le7KOX91w7U`; generation then returned the honest upstream audio-delivery error because YouTube blocked the Worker egress.
- bilibili presented a Cloudflare HTML challenge to Worker egress. ClipQuest now reports a source-specific availability error instead of treating challenge HTML as JSON.
- To isolate the lesson UI from those upstream delivery blocks, a seven-question AP Biology bank was seeded only for the disposable QA learner. It was completed in live Chrome at 100%.
- The live lesson covered disabled Check state, mouse selection, keyboard Tab/Space selection, incorrect feedback, retry, correct feedback, true/false, multiple choice, ordering with Move up controls, progress, and completion.

This proves the deployed lesson and admin interfaces; it does not claim that current Cloudflare egress can always retrieve YouTube audio or bilibili metadata.

### Admin acceptance

The owner QA session opened every production management route through real clicks:

- Overview
- People
- Processing
- Lessons
- Audit log
- System

The isolated learner was suspended with a QA reason and immediately restored. Both successful actions appeared in the audit log. No real user, lesson, job, or credential was modified.

### Final automated checks

```text
Formatting:       pass
Lint:             pass
API tests:        24/24
App tests:        17/17
Contract tests:    6/6
Total tests:      47/47
Workspace types:  pass
Expo web export:  pass, 29 routes
Worker dry build: pass
Cloudflare deploy: pass
```

## Scope, safety, and final status

This continuation reviewed the existing handoff, tested the deployed web app through Chrome Computer Use at desktop and mobile widths, used Wrangler for Cloudflare inspection and disposable account provisioning, fixed the reproducible application defects found, deployed the resulting Worker/assets, and stopped immediately when the user requested a handoff and push.

- Shadowrocket and all system proxy/network settings were untouched.
- No production secret value or QA password was printed, committed, put in shell history, or included in this report.
- Two random verified, age-confirmed QA identities were created through Wrangler/D1. A prior QA fixture also remains.
- The latest live Worker version tested was `04336ce8-810c-44b7-963c-a470b7a25ee4`.
- The final clean mobile-generation rerun, controlled DeepSeek generation, 390 px rerun, and latest desktop rerun were interrupted and remain explicitly open.

## Defects fixed

### Web runtime, routing, and browser hardening

1. The Better Auth Expo bridge called native synchronous SecureStore APIs in browsers. Web now uses same-origin cookie credentials without the native storage getter; a regression test covers it.
2. Dynamic deep links previously returned platform 404s or hydrated home markup as the wrong route. The Worker now serves matching Expo-exported route shells for `/create/:id`, `/generation/:id`, and `/quiz/:id`; unknown routes receive the generated 404.
3. Generated `@expo` asset paths are encoded during finalization, avoiding an unnecessary production redirect.
4. CSP now covers Expo's exact hydration script by build-time SHA-256 hash, uses Zod's supported JITless configuration, retains only the WebAssembly capability needed by local Whisper, and permits only the documented Cloudflare analytics endpoints.
5. Shared web inputs receive stable generated HTML IDs, fixing Chrome form-field diagnostics while retaining accessible labels.
6. Route responses carry hardened CSP, COEP, COOP, CORP, no-sniff/referrer/permissions policies, and an explicit no-transform cache policy.

### Authentication and account safety

1. Verification and recovery email sends are awaited so delivery-provider failures are not falsely reported as success.
2. Verification no longer invokes an unsupported native-cookie auto-sign-in callback.
3. Age-13 confirmation is enforced by a server database hook in addition to the UI.
4. Better Auth rate limiting is backed by D1, with schema alignment supplied by migration `0005`.
5. Account deletion requires password confirmation and removes the user's private transcript/thumbnail objects before relational deletion.
6. Sign-up, sign-in, verification, recovery, reset, and deletion screens guard duplicate submits, disable pending inputs, and show localized recovery errors.
7. Web auth persistence was verified by signing in with a Wrangler-created mobile QA identity and reloading.

### Quiz correctness and mastery

1. Failed initial attempts no longer schedule review.
2. An initial score of at least 80% schedules review; only a later review score of at least 80% reaches Mastered.
3. Old pending reviews are closed before rescheduling to prevent duplicates.
4. Resume responses include mastery and are reconciled with server authority before cached client state is used.
5. Out-of-sequence answers trigger authoritative resume rather than corrupting progress.
6. Ordering prompts start shuffled and require learner interaction.
7. Answer controls are disabled during submission to prevent duplicates.

### Generation correctness and recovery

1. Transcript objects use immutable per-generation keys; grading resolves the transcript associated with that exact generation.
2. Persistent per-user idempotency recovers a lost create response without retranscribing or creating duplicate jobs.
3. D1 is the single job-state authority; the prior racing KV status cache was removed.
4. Retry requeues the stored transcript. Cancellation persists and is checked between AI phases.
5. Queue attempts increment once, ownership is checked before disclosure, and final quiz/job writes are batched with cancellation cleanup.
6. The client persists job identity, resumes polling after restart, aborts stale listeners, exposes retry/cancel, and enforces a 15-minute deadline.
7. Browser transcription explicitly caps input at 20 minutes because compressed and PCM buffers coexist; native retains the 90-minute limit.
8. A valid unseen idempotency key now returns `200 null` instead of an expected 404 that polluted the browser console.
9. Structured API/media errors are parsed for display, including the actionable YouTube-delivery message.
10. Immediate web navigation blurs the active control first to avoid hiding a focused button under an `aria-hidden` route tree.

### YouTube source handling

1. Watch, `youtu.be`, Shorts, Live, and Embed variants are accepted; malformed IDs and lookalike hosts are rejected.
2. The adapter uses an iOS InnerTube client for rich metadata/caption tracks and media because it was the usable client in local source diagnostics.
3. Direct `json3` caption URLs are HTTPS/YouTube-host validated, response-size bounded, parsed into timed segments, and selected with English/Chinese preference.
4. Metadata falls back through a bounded public watch-page parser and then bounded oEmbed, retaining safe reason classifications only.
5. The watch-page balanced-JSON extractor handles braces inside quoted strings and is regression-tested.
6. Reimports during upstream throttling preserve a previously known nonzero duration and source language.
7. Audio-delivery failure now tells the learner that YouTube temporarily blocked delivery and suggests retrying or using another video.

### API, mobile quality, and dependency surface

1. API fixed-window limits use atomic D1 upsert plus expiry cleanup rather than KV read/modify/write races.
2. `/health` reveals only configuration-presence booleans and reports ready only when auth, generation, and email are configured.
3. Auth shells stack below 900 px; 390 px and 320 px layouts no longer clip the left panel.
4. Scroll, keyboard insets, safe-area padding, wrapping footers, responsive cards/thumbnails, and minimum touch targets were corrected.
5. Quiz, completion, generation, settings, and long auth screens remain reachable on short mobile viewports.
6. User-facing recovery/settings fallbacks are localized in English and Simplified Chinese.
7. Removing unused `@cloudflare/vitest-pool-workers` dropped its obsolete Wrangler/Miniflare dependency chain. No forced Expo-breaking audit fix was applied.

## Verification evidence

### 2026-08-03 exact production demo

- Chrome completed the exact URL `https://www.youtube.com/watch?v=BjRvQbWsTfM&list=PLmpmyPywZ440vPqpAPeUkE-TeKifbS45W` from paste through a rendered five-question English quiz.
- The final visible click-to-question measurement was 9.04 seconds. The matching Wrangler trace recorded 2.839 seconds for the model phase and 4.060 seconds for the complete generation job, with no retry.
- A second live Chrome pass completed `https://www.youtube.com/watch?v=HZVwZa2vOZ8` (Unit 2) after reproducing its original `youtube_unavailable` failure. The accepted pass imported the cached 734-segment transcript and rendered question 1 in 8.499 seconds from `Create my quiz`; the matching generation job completed in 3.946 seconds.
- The accepted Unit 2 import was a private R2 cache hit: 650 ms Worker time and 1.641 seconds from the Chrome click to the visible `Video ready` screen.
- Short questions are generated concurrently in bounded groups of five. Classification runs alongside the first group, invalid/slow item responses have grounded local fallbacks, and normal jobs start immediately with the queue retained as the retry path.
- Structured logs now expose video-import acceptance, cache hit/miss/failure timing, adapter timing, caption counts, queue/fast-path lifecycle, transcript load, each model request, validation/fallback, persistence, total elapsed time, and retry disposition without transcript text or credentials. Observe them with `npx wrangler tail clipquest --format pretty` from `apps/api`.
- All 15 videos in the supplied AP CSA playlist are backed by validated normalized private R2 objects under `source-cache/youtube/`. `apps/api/scripts/cache-youtube-sources.ts` can build the same first-party cache format for additional authorized URLs or playlists; malformed or absent cache data falls back to the normal adapter.
- The quiz route now renders the question saved by the successful start response immediately instead of blocking on a redundant resume request. Server-side out-of-sequence handling still resynchronizes a stale local question.
- The accepted Worker version is `2f5fad85-8aa2-4118-81e5-a6e7669afbf6`.

### Automated and build checks

The latest post-fix run passed:

```text
API:             28 tests in 9 files
App:             17 tests in 8 files
Contracts:        6 tests
Total:           51 tests
API typecheck:    pass
App typecheck:    pass
Expo web export:  pass, 29 routes
Worker dry build: pass
```

The accepted production build used `entry-930a1c2bb5760c43d36dfde94091ea33.js`, and the final deployment uploaded three changed assets.

### Cloudflare and Wrangler

- Remote D1 migrations `0002` through `0005` are applied.
- Exactly four expected Worker secret names remain: DeepSeek, Resend, Better Auth, and YouTube-credential encryption. Values were never read or recorded.
- `/health` returned ready with all four configuration-presence groups true.
- Two random QA identities were provisioned through Wrangler/D1 with verified email and age confirmation.
- Live route probes returned 200 for create/generation/quiz shells and 404 for a missing route.
- The sign-in response included the expected cache and cross-origin/security headers and no unintended analytics injection.
- Wrangler tail and some control-plane requests intermittently failed with TLS resets/521/522 responses. Retrying normal operations succeeded; no network workaround was attempted.

### Computer Use

Completed evidence:

- Local production build at 390×720: welcome and sign-up remained within width; long content scrolled to the footer.
- Local production build at 320×720: wrapped title, fields, buttons, and footer remained reachable.
- Local dynamic deep links at 320 px rendered the intended route/expired state instead of a platform 404, with matching hydration and no React error.
- Live production at 320×720: a Wrangler-created mobile QA identity signed in, reached Home, and remained authenticated after reload.
- Live import of `https://www.youtube.com/watch?v=aircAruvnKk` produced the exact title `But what is a neural network? | Deep learning chapter 1`.
- Remote D1 retained the exact 1,120-second duration and English language for video `aa502f5a-12bb-4e52-beb0-db5d778bcb0d` even after a throttled reimport.
- Setup controls visibly selected watched/English/short and advanced through Getting video to Preparing audio.

Interrupted evidence:

- The pre-fix live generation bundle emitted an idempotency 404, a focused-button `aria-hidden` warning, and the upstream media 502. Source fixes for all three were deployed.
- After the newest bundle was loaded and the console cleared, mobile Home rendered. The only observed new console failure was a transient `site.webmanifest` request ending in `net::ERR_CONNECTION_CLOSED`.
- The user stopped the run before the clean post-deployment generation transition could prove the first two console defects absent and the improved 502 message present.
- The latest deployed bundle was not rerun at 390 px or desktop after that stop.

## Upstream YouTube limitation

During this run, YouTube challenged Cloudflare's shared Worker egress with its bot/sign-in gate. Local diagnostics established that rich InnerTube metadata and caption-track descriptors are available when the source permits the request, and iOS was the only tested client that could stream an initial audio chunk. From the current live Worker egress, caption content and audio bytes were still blocked for the chosen test video.

The deployed fallback now preserves exact title/duration/language whenever possible and degrades to bounded public metadata rather than a generic title. The accepted demo uses a validated private first-party source cache; uncached videos still use the normal live adapter. The UI provides an actionable retry/alternate-video error. A browser-derived PoToken is not a safe general remedy because tokens are tied to generating client/egress context, and mobile uses DroidGuard. Do not collect user cookies, alter Shadowrocket/system routing, weaken the local-transcription privacy boundary, or send raw audio to an unapproved third party.

## Remaining acceptance work

These are not all source defects, and none should be reported as completed:

1. Repeat the latest Home, Library, Settings, auth, create, generation, and quiz checks at 390 px and desktop.
2. Confirm Resend verification and recovery delivery to a disposable real inbox.
3. Exercise captionless WebGPU, browser WASM fallback, Android, and iOS transcription on real devices, including cache removal, interruption resume, low-memory rejection, and temporary-audio cleanup.
4. Deliver a real push notification after linking valid Expo project/device credentials.
5. Produce production-signed Android and iOS artifacts when distribution credentials are available.
6. Clean up disposable production QA records after acceptance is complete.

The controlled web paste-to-first-question path is accepted for the exact demo URL. Broader paste-to-Mastered, device, email, push, signing, and uncached-source acceptance remains open.
